Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const attributes = require('@sentry/conventions/attributes');
const core = require('@sentry/core');
const helpers = require('./helpers.js');
const AttributeNames = require('./vendored/enums/AttributeNames.js');
const NestType = require('./vendored/enums/NestType.js');

const NESTJS_COMPONENT = "@nestjs/core";
function getAppCreationSpanOptions(moduleVersion, moduleName) {
  return {
    name: "Create Nest App",
    op: `${NestType.NestType.APP_CREATION}.nestjs`,
    attributes: {
      component: NESTJS_COMPONENT,
      [core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: helpers.httpOrigin(),
      [AttributeNames.AttributeNames.TYPE]: NestType.NestType.APP_CREATION,
      [AttributeNames.AttributeNames.VERSION]: moduleVersion || void 0,
      [AttributeNames.AttributeNames.MODULE]: moduleName || void 0
    }
  };
}
function wrapRouteHandler(callback, moduleVersion) {
  if (helpers.isWrapped(callback)) {
    return callback;
  }
  const spanName = callback.name || "anonymous nest handler";
  const attributes = {
    component: NESTJS_COMPONENT,
    [core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: helpers.httpOrigin(),
    [AttributeNames.AttributeNames.TYPE]: NestType.NestType.REQUEST_HANDLER,
    [AttributeNames.AttributeNames.CALLBACK]: callback.name,
    [AttributeNames.AttributeNames.VERSION]: moduleVersion || void 0
  };
  const wrapped = function(...args) {
    return core.startSpan(
      { name: spanName, op: `${NestType.NestType.REQUEST_HANDLER}.nestjs`, attributes },
      () => callback.apply(this, args)
    );
  };
  if (callback.name) {
    Object.defineProperty(wrapped, "name", { value: callback.name });
  }
  helpers.copyReflectMetadata(callback, wrapped);
  helpers.markWrapped(wrapped);
  return wrapped;
}
function wrapRequestContextHandler(handler, instanceName, callbackName, moduleVersion) {
  const spanName = callbackName ? `${instanceName}.${callbackName}` : instanceName;
  const wrapped = function(...handlerArgs) {
    const req = handlerArgs[0] || {};
    const httpRoute = req.route?.path || req.routeOptions?.url || req.routerPath;
    const attributes$1 = {
      component: NESTJS_COMPONENT,
      [core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: helpers.httpOrigin(),
      [AttributeNames.AttributeNames.TYPE]: NestType.NestType.REQUEST_CONTEXT,
      [AttributeNames.AttributeNames.CONTROLLER]: instanceName,
      [AttributeNames.AttributeNames.CALLBACK]: callbackName,
      [AttributeNames.AttributeNames.VERSION]: moduleVersion || void 0,
      [attributes.HTTP_ROUTE]: httpRoute || void 0,
      ["http.method"]: req.method || void 0,
      ["http.url"]: req.originalUrl || req.url || void 0
    };
    return core.startSpan(
      { name: spanName, op: `${NestType.NestType.REQUEST_CONTEXT}.nestjs`, attributes: attributes$1 },
      () => handler.apply(this, handlerArgs)
    );
  };
  helpers.markWrapped(wrapped);
  return wrapped;
}

exports.getAppCreationSpanOptions = getAppCreationSpanOptions;
exports.wrapRequestContextHandler = wrapRequestContextHandler;
exports.wrapRouteHandler = wrapRouteHandler;
//# sourceMappingURL=wrap-route.js.map
