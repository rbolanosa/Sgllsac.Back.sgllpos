Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const core = require('@sentry/core');
const orchestrion = require('@sentry/server-utils/orchestrion');

const SENTRY_WRAPPED = /* @__PURE__ */ Symbol.for("sentry.nestjs.wrapped");
function isWrapped(fn) {
  return !!fn[SENTRY_WRAPPED];
}
function markWrapped(fn) {
  fn[SENTRY_WRAPPED] = true;
}
function copyReflectMetadata(from, to) {
  const R = Reflect;
  if (typeof R.getMetadataKeys !== "function" || typeof R.getMetadata !== "function" || typeof R.defineMetadata !== "function") {
    return;
  }
  for (const key of R.getMetadataKeys(from)) {
    R.defineMetadata(key, R.getMetadata(key, from), to);
  }
}
function isTargetPatched(target, flag) {
  if (target[flag]) {
    return true;
  }
  core.addNonEnumerableProperty(target, flag, true);
  return false;
}
function middlewareOrigin(componentType) {
  const base = orchestrion.isOrchestrionInjected() ? "auto.middleware.orchestrion.nestjs" : "auto.middleware.nestjs";
  return componentType ? `${base}.${componentType}` : base;
}
function httpOrigin() {
  return orchestrion.isOrchestrionInjected() ? "auto.http.orchestrion.nestjs" : "auto.http.otel.nestjs";
}
function eventOrigin() {
  return orchestrion.isOrchestrionInjected() ? "auto.event.orchestrion.nestjs" : "auto.event.nestjs";
}
function bullmqOrigin() {
  return orchestrion.isOrchestrionInjected() ? "auto.queue.orchestrion.nestjs.bullmq" : "auto.queue.nestjs.bullmq";
}
function getMiddlewareSpanOptions(target, name = void 0, componentType = void 0) {
  return {
    name: name ?? target.name ?? "unknown",
    attributes: {
      [core.SEMANTIC_ATTRIBUTE_SENTRY_OP]: "middleware.nestjs",
      [core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: middlewareOrigin(componentType)
    }
  };
}
function getEventSpanOptions(event) {
  return {
    name: `event ${event}`,
    attributes: {
      [core.SEMANTIC_ATTRIBUTE_SENTRY_OP]: "event.nestjs",
      [core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: eventOrigin()
    },
    forceTransaction: true
  };
}
function getBullMQProcessSpanOptions(queueName) {
  return {
    name: `${queueName} process`,
    attributes: {
      [core.SEMANTIC_ATTRIBUTE_SENTRY_OP]: "queue.process",
      [core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: bullmqOrigin(),
      "messaging.system": "bullmq",
      "messaging.destination.name": queueName
    },
    forceTransaction: true
  };
}
function instrumentObservable(observable, activeSpan) {
  if (activeSpan) {
    observable.subscribe = new Proxy(observable.subscribe, {
      apply: (originalSubscribe, thisArgSubscribe, argsSubscribe) => {
        return core.withActiveSpan(activeSpan, () => {
          const subscription = originalSubscribe.apply(thisArgSubscribe, argsSubscribe);
          subscription.add(() => activeSpan.end());
          return subscription;
        });
      }
    });
  }
}
function getNextProxy(next, span, prevSpan) {
  return new Proxy(next, {
    apply: (originalNext, thisArgNext, argsNext) => {
      span.end();
      if (prevSpan) {
        return core.withActiveSpan(prevSpan, () => {
          return Reflect.apply(originalNext, thisArgNext, argsNext);
        });
      } else {
        return Reflect.apply(originalNext, thisArgNext, argsNext);
      }
    }
  });
}

exports.copyReflectMetadata = copyReflectMetadata;
exports.getBullMQProcessSpanOptions = getBullMQProcessSpanOptions;
exports.getEventSpanOptions = getEventSpanOptions;
exports.getMiddlewareSpanOptions = getMiddlewareSpanOptions;
exports.getNextProxy = getNextProxy;
exports.httpOrigin = httpOrigin;
exports.instrumentObservable = instrumentObservable;
exports.isTargetPatched = isTargetPatched;
exports.isWrapped = isWrapped;
exports.markWrapped = markWrapped;
//# sourceMappingURL=helpers.js.map
