Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const instrumentation = require('@opentelemetry/instrumentation');
const core = require('@sentry/core');
const wrapHandlers = require('./wrap-handlers.js');

const supportedVersions = [">=2.0.0"];
const COMPONENT = "@nestjs/schedule";
class SentryNestScheduleInstrumentation extends instrumentation.InstrumentationBase {
  constructor(config = {}) {
    super("sentry-nestjs-schedule", core.SDK_VERSION, config);
  }
  /**
   * Initializes the instrumentation by defining the modules to be patched.
   */
  init() {
    const moduleDef = new instrumentation.InstrumentationNodeModuleDefinition(COMPONENT, supportedVersions);
    moduleDef.files.push(this._getDecoratorFileInstrumentation("Cron", "cron", wrapHandlers.MECHANISM_CRON, supportedVersions));
    moduleDef.files.push(
      this._getDecoratorFileInstrumentation("Interval", "interval", wrapHandlers.MECHANISM_INTERVAL, supportedVersions)
    );
    moduleDef.files.push(
      this._getDecoratorFileInstrumentation("Timeout", "timeout", wrapHandlers.MECHANISM_TIMEOUT, supportedVersions)
    );
    return moduleDef;
  }
  /**
   * Wraps a schedule decorator (`@Cron`/`@Interval`/`@Timeout`).
   */
  _getDecoratorFileInstrumentation(exportName, fileName, mechanismType, versions) {
    return new instrumentation.InstrumentationNodeModuleFile(
      `@nestjs/schedule/dist/decorators/${fileName}.decorator.js`,
      versions,
      (moduleExports) => {
        if (instrumentation.isWrapped(moduleExports[exportName])) {
          this._unwrap(moduleExports, exportName);
        }
        this._wrap(moduleExports, exportName, this._createWrapDecorator(mechanismType));
        return moduleExports;
      },
      (moduleExports) => {
        this._unwrap(moduleExports, exportName);
      }
    );
  }
  /**
   * Creates a wrapper function for a schedule decorator.
   */
  _createWrapDecorator(mechanismType) {
    return function wrapDecorator(original) {
      return function wrappedDecorator(...decoratorArgs) {
        const decoratorResult = original(...decoratorArgs);
        return (target, propertyKey, descriptor) => {
          wrapHandlers.patchMethodDescriptor(
            target,
            propertyKey,
            descriptor,
            (handler) => wrapHandlers.wrapScheduleHandler(handler, mechanismType)
          );
          return decoratorResult(target, propertyKey, descriptor);
        };
      };
    };
  }
}

exports.SentryNestScheduleInstrumentation = SentryNestScheduleInstrumentation;
//# sourceMappingURL=sentry-nest-schedule-instrumentation.js.map
