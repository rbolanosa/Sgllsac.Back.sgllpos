import { HTTP_ROUTE } from '@sentry/conventions/attributes';
import { SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, startSpan } from '@sentry/core';
import { httpOrigin, isWrapped, copyReflectMetadata, markWrapped } from './helpers.js';
import { AttributeNames } from './vendored/enums/AttributeNames.js';
import { NestType } from './vendored/enums/NestType.js';

const NESTJS_COMPONENT = "@nestjs/core";
function getAppCreationSpanOptions(moduleVersion, moduleName) {
  return {
    name: "Create Nest App",
    op: `${NestType.APP_CREATION}.nestjs`,
    attributes: {
      component: NESTJS_COMPONENT,
      [SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: httpOrigin(),
      [AttributeNames.TYPE]: NestType.APP_CREATION,
      [AttributeNames.VERSION]: moduleVersion || void 0,
      [AttributeNames.MODULE]: moduleName || void 0
    }
  };
}
function wrapRouteHandler(callback, moduleVersion) {
  if (isWrapped(callback)) {
    return callback;
  }
  const spanName = callback.name || "anonymous nest handler";
  const attributes = {
    component: NESTJS_COMPONENT,
    [SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: httpOrigin(),
    [AttributeNames.TYPE]: NestType.REQUEST_HANDLER,
    [AttributeNames.CALLBACK]: callback.name,
    [AttributeNames.VERSION]: moduleVersion || void 0
  };
  const wrapped = function(...args) {
    return startSpan(
      { name: spanName, op: `${NestType.REQUEST_HANDLER}.nestjs`, attributes },
      () => callback.apply(this, args)
    );
  };
  if (callback.name) {
    Object.defineProperty(wrapped, "name", { value: callback.name });
  }
  copyReflectMetadata(callback, wrapped);
  markWrapped(wrapped);
  return wrapped;
}
function wrapRequestContextHandler(handler, instanceName, callbackName, moduleVersion) {
  const spanName = callbackName ? `${instanceName}.${callbackName}` : instanceName;
  const wrapped = function(...handlerArgs) {
    const req = handlerArgs[0] || {};
    const httpRoute = req.route?.path || req.routeOptions?.url || req.routerPath;
    const attributes = {
      component: NESTJS_COMPONENT,
      [SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN]: httpOrigin(),
      [AttributeNames.TYPE]: NestType.REQUEST_CONTEXT,
      [AttributeNames.CONTROLLER]: instanceName,
      [AttributeNames.CALLBACK]: callbackName,
      [AttributeNames.VERSION]: moduleVersion || void 0,
      [HTTP_ROUTE]: httpRoute || void 0,
      ["http.method"]: req.method || void 0,
      ["http.url"]: req.originalUrl || req.url || void 0
    };
    return startSpan(
      { name: spanName, op: `${NestType.REQUEST_CONTEXT}.nestjs`, attributes },
      () => handler.apply(this, handlerArgs)
    );
  };
  markWrapped(wrapped);
  return wrapped;
}

export { getAppCreationSpanOptions, wrapRequestContextHandler, wrapRouteHandler };
//# sourceMappingURL=wrap-route.js.map
