import { getActiveSpan, startSpanManual, startSpan, withActiveSpan, isThenable, startInactiveSpan } from '@sentry/core';
import { isTargetPatched, getMiddlewareSpanOptions, getNextProxy, instrumentObservable } from './helpers.js';

function patchInterceptor(target, intercept, seenContexts) {
  return new Proxy(intercept, {
    apply: (originalIntercept, thisArg, argsIntercept) => {
      const context = argsIntercept[0];
      const next = argsIntercept[1];
      const parentSpan = getActiveSpan();
      let afterSpan;
      if (!context || !next || typeof next.handle !== "function" || target.name === "SentryTracingInterceptor") {
        return originalIntercept.apply(thisArg, argsIntercept);
      }
      return startSpanManual(getMiddlewareSpanOptions(target, void 0, "interceptor"), (beforeSpan) => {
        next.handle = new Proxy(next.handle, {
          apply: (originalHandle, thisArgHandle, argsHandle) => {
            beforeSpan.end();
            const run = () => {
              const handleReturn = Reflect.apply(originalHandle, thisArgHandle, argsHandle);
              if (!seenContexts.has(context)) {
                seenContexts.add(context);
                afterSpan = startInactiveSpan(
                  getMiddlewareSpanOptions(target, "Interceptors - After Route", "interceptor")
                );
              }
              return handleReturn;
            };
            return parentSpan ? withActiveSpan(parentSpan, run) : run();
          }
        });
        let returned;
        try {
          returned = originalIntercept.apply(thisArg, argsIntercept);
        } catch (e) {
          beforeSpan.end();
          afterSpan?.end();
          throw e;
        }
        if (isThenable(returned)) {
          return returned.then(
            (observable) => {
              if (afterSpan) {
                instrumentObservable(observable, afterSpan);
              } else {
                beforeSpan.end();
              }
              return observable;
            },
            (e) => {
              beforeSpan.end();
              afterSpan?.end();
              throw e;
            }
          );
        }
        if (!afterSpan) {
          beforeSpan.end();
          return returned;
        }
        if (typeof returned.subscribe === "function") {
          instrumentObservable(returned, afterSpan);
        }
        return returned;
      });
    }
  });
}
function patchInjectableTarget(target, seenContexts) {
  const proto = target?.prototype;
  if (!proto || target.__SENTRY_INTERNAL__ || isTargetPatched(target, "sentryPatchedInjectable")) {
    return;
  }
  if (typeof proto.use === "function") {
    proto.use = new Proxy(proto.use, {
      apply: (originalUse, thisArgUse, argsUse) => {
        const [req, res, next] = argsUse;
        if (!req || !res || !next || typeof next !== "function") {
          return originalUse.apply(thisArgUse, argsUse);
        }
        const prevSpan = getActiveSpan();
        return startSpanManual(getMiddlewareSpanOptions(target), (span) => {
          const nextProxy = getNextProxy(next, span, prevSpan);
          const rest = argsUse.slice(3);
          return originalUse.apply(thisArgUse, [req, res, nextProxy, ...rest]);
        });
      }
    });
  }
  if (typeof proto.canActivate === "function") {
    proto.canActivate = new Proxy(proto.canActivate, {
      apply: (originalCanActivate, thisArg, args) => {
        if (!args[0]) {
          return originalCanActivate.apply(thisArg, args);
        }
        return startSpan(
          getMiddlewareSpanOptions(target, void 0, "guard"),
          () => originalCanActivate.apply(thisArg, args)
        );
      }
    });
  }
  if (typeof proto.transform === "function") {
    proto.transform = new Proxy(proto.transform, {
      apply: (originalTransform, thisArg, args) => {
        if (!args[0] || !args[1]) {
          return originalTransform.apply(thisArg, args);
        }
        return startSpan(
          getMiddlewareSpanOptions(target, void 0, "pipe"),
          () => originalTransform.apply(thisArg, args)
        );
      }
    });
  }
  if (typeof proto.intercept === "function") {
    proto.intercept = patchInterceptor(
      target,
      proto.intercept,
      seenContexts
    );
  }
}
function patchCatchTarget(target) {
  const proto = target?.prototype;
  if (!proto || typeof proto.catch !== "function" || target.__SENTRY_INTERNAL__ || isTargetPatched(target, "sentryPatchedCatch")) {
    return;
  }
  proto.catch = new Proxy(proto.catch, {
    apply: (originalCatch, thisArg, args) => {
      const [exception, host] = args;
      if (!exception || !host) {
        return originalCatch.apply(thisArg, args);
      }
      return startSpan(
        getMiddlewareSpanOptions(target, void 0, "exception_filter"),
        () => originalCatch.apply(thisArg, args)
      );
    }
  });
}

export { patchCatchTarget, patchInjectableTarget };
//# sourceMappingURL=wrap-components.js.map
