import type { InstrumentationConfig } from '@opentelemetry/instrumentation';
import { InstrumentationBase, InstrumentationNodeModuleDefinition } from '@opentelemetry/instrumentation';
/**
 * Custom instrumentation for nestjs schedule module.
 *
 * This hooks into the `@Cron`, `@Interval`, and `@Timeout` decorators, which
 * are applied on scheduled task handlers.
 */
export declare class SentryNestScheduleInstrumentation extends InstrumentationBase {
    constructor(config?: InstrumentationConfig);
    /**
     * Initializes the instrumentation by defining the modules to be patched.
     */
    init(): InstrumentationNodeModuleDefinition;
    /**
     * Wraps a schedule decorator (`@Cron`/`@Interval`/`@Timeout`).
     */
    private _getDecoratorFileInstrumentation;
    /**
     * Creates a wrapper function for a schedule decorator.
     */
    private _createWrapDecorator;
}
//# sourceMappingURL=sentry-nest-schedule-instrumentation.d.ts.map